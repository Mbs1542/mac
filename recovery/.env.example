# Fill and copy to .env (macOS host)
CF_API_EMAIL=your-cloudflare-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-api-token
# Generate password hash on Mac: openssl passwd -apr1 'your-password'
TRAEFIK_USER=admin
TRAEFIK_PASSWORD=$apr1$replace_with_hash
